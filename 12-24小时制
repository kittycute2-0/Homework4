#include <iostream>
using namespace std;
int main() 
{
    int h,m,H;
	scanf("%d:%d",&h,&m);
	if (h>=12)
	 {if (h==12) printf("%d:%d PM",h, m);
        else 
		{H=h-12;
	     printf("%d:%d PM",H,m);
	       }
	 }
	else 
	 printf("%d:%d AM",h,m);
	return 0;
}
